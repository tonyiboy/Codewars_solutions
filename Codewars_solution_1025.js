// Get ASCII value of a character.

// For the ASCII table you can refer to http://www.asciitable.com/

function getASCII(c){
    let index = 0;
    
    let result = c.charCodeAt(index);
      return result
    }